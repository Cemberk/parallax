#!/usr/bin/env python3
"""PRLX CLI — thin wrapper for development use.

After ``pip install prlx``, use the installed ``prlx`` console script instead.
This file is only used when running directly from the source tree.
"""
import sys
from pathlib import Path

# Ensure the local python/ package is importable
sys.path.insert(0, str(Path(__file__).resolve().parent / "python"))

from prlx.cli import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main())
